# Walk-Forward Dual-Bucket Backtest Report (v4 spec-faithful, round 185)

_Generated by `scripts/walk_forward_dual_bucket.py` (round 185, v4)_

**v4 spec** (advisor round 184 final, after round 183 push-back ACK):
- Single SHARED `SimulatedBroker(¥200k)` (matches real QMT 8886933837 single-account)
- **OOS path = rebalance** (sell exits + buy new, action-tagged SELL_OOS / BUY_OOS)
  - matches production `intraday_plan.generate_orders` which delegates to
    `daily_report.generate_order_list` (full rebalance, NOT only-buy)
- No merged-cap check (production has none)
- No `ArmBBudgetTracker` cap in backtest (advisor 179: cap is prod risk control,
  not part of model-alpha evaluation; prod cap ¥20k unchanged)
- Same `target_pos_pct=0.70` sizing for EOD and OOS (apples-to-apples)
- Post-hoc conflict detection from `broker.trade_log` (round 185 refined Type A/B)

**Conflict types (round 184)**:
- **Type A**: `BUY_OOS X` on D 14:30 → `SELL X` on D+1 09:25 (OOS 持仓被 EOD 隔夜踩)
- **Type B**: `BUY X` on D 09:25 (EOD) → `SELL_OOS X` on D 14:30 (EOD 早买, OOS 下午就卖)

## Inputs
- Window: 2025-09-01 → 2026-04-28
- Seeds: 3
- Initial capital: ¥200,000
- Top-K per bucket: 10
- OOS daily new-buy cap: ¥20,000
- EOD model: `data/blend_*.lgb` (n2c, FACTOR_COLUMNS 64-col)
- OOS model: `data/intraday_blend_*.lgb` (c2c, INTRADAY_FEATURE_COLS 68-col)

## Results (mean ± std across 3 seeds)

| Mode | Final NAV | Return | Max DD | Friction (¥) | Turnover (#trades) | Conflicts (A+B) | Pick overlap (Jaccard) |
|---|---:|---:|---:|---:|---:|---:|---:|
| eod_only | ¥225,242 ± 0 | +12.62% | 4.89% | ¥8,180 | 737 | 0 (A=0 / B=0) | 0.0% |
| oos_only | ¥230,293 ± 0 | +15.15% | 7.66% | ¥9,400 | 820 | 0 (A=0 / B=0) | 0.0% |
| dual | ¥252,354 ± 0 | +26.18% | 6.60% | ¥21,177 | 1859 | 56 (A=0 / B=56) | 3.1% |

## Net alpha delta
- `dual_NAV − max(EOD_only, OOS_only)` = **¥+22,061** (¥+1103bps of capital)
- `dual_NAV − (EOD_only + OOS_only)/2` = **¥+24,586** (¥+1229bps of capital)

## Friction breakdown (mean across seeds)

| Mode | Commission | Slippage | Stamp tax | Total |
|---|---:|---:|---:|---:|
| eod_only | ¥2,898 | ¥2,898 | ¥2,383 | ¥8,180 |
| oos_only | ¥3,332 | ¥3,332 | ¥2,735 | ¥9,400 |
| dual | ¥7,487 | ¥7,487 | ¥6,203 | ¥21,177 |

## Reading the comparison fairly (v3)
All three modes share the SAME total capital (¥200,000) in a SINGLE shared broker (matches production QMT single-account):
- `eod_only`: only EOD rebalance runs (sell + buy). OOS path disabled.
- `oos_only`: only OOS only-buy runs at 14:30, capped at ¥20,000/day. **No selling** (production `intraday_plan` has 0 sell logic). Cash drains as positions accumulate; nothing rebalances stale picks.
- `dual`: EOD rebalance at 09:25 + OOS only-buy at 14:30 on the same broker. OOS competes with EOD for the same `broker.cash`. EOD's 09:25 sells may dump positions OOS just bought yesterday — the round-trip the user worried about.

`dual − max(solo)` answers: *does adding OOS to EOD net-add alpha, or does the round-trip eat it?* If negative, OOS is a drag on EOD; if positive, the two strategies complement.

## Conflict definition (v3, post-hoc from `broker.trade_log`)
- **Type A**: OOS buys X on D 14:30 + EOD sells X on D 09:25 (same day, EOD already sold this morning, OOS rebuys at 14:30 — user's headline worry)
- **Type B**: OOS bought X on D-1 14:30 + EOD sells X on D 09:25 (overnight: OOS yesterday's pick, EOD dumps it next morning)

Both count toward the strict `Conflicts (A+B)` total. v3 doesn't *prevent* conflicts — production architecture doesn't either — the cost is already baked into `dual NAV`.

## Caveats
- 8-month in-sample window — sample size is small; treat as directional, not definitive
- **Seed std is 0** for all modes: the pipeline (factors → predict → top-K → execute) is deterministic. Different seeds label runs but do not produce variation. A real ± needs a stochastic source (random tie-break, bootstrap, or train-time seed). Out of MVP scope.
- `data/intraday_blend_*.lgb` was trained 2026-05-27, BEFORE the n2c label upgrade (round 162). This is the *currently-shipped* OOS model so its drag is what user asked us to measure.
- OOS panel uses D-1 factor row + 4 INTRADAY_EXTRA_COLUMNS proxies (MVP). Production intraday_plan re-derives 64 TECHNICAL_COLUMNS from morning bars — this would tighten OOS scoring
- OOS execution price = 1m close at 14:29 from `data/intraday_1m_qfq/`; real fills are next-second auction (~5bps optimistic)
- `oos_only` accumulates positions forever (no sell); over 8 months this drags down NAV when bad picks turn worse. Production wouldn't run OOS in isolation — this mode is reference only.

## Rule reminders
- Rule #4: pure research, `--skip-update` style, no prod model touched
- Rule #11: PIT — EOD ≤ D close (executes D+1 open), OOS ≤ D 14:29
- Rule #1: report + json output NOT in git; this script IS in git